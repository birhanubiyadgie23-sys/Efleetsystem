rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Auth ላደረጉ ተጠቃሚዎች በሙሉ ማንበብ እና መጻፍ ይፈቅዳል
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
  }
}